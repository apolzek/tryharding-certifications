# Exercício 05: SLO de latência "99% das requisições em até 300ms".
# Métrica: histograma CLASSIC http_request_duration_seconds (_bucket{le}, _count, _sum), bucket le="0.3" existe.
#   job:slo_latency_slow_per_request:ratio_rate5m   -> fração de requisições LENTAS (> 300ms), via razão de buckets
#   job:slo_latency_slow_per_request:ratio_rate1h   -> idem, 1h
#   job:slo_latency_good_per_request:fraction_rate5m -> fração RÁPIDA (<= 300ms), via histogram_fraction()
#   alerta SLOLatencyBudgetBurnFast: burn > 14.4 em 1h E 5m (budget = 1%), for: 2m, labels severity=page
groups:
  - name: slo-checkout-latency
    rules: []   # TODO
